// install node js -> https://nodejs.org/en/download/current

// 1st command is 'npm init' (use -y to give answers by default)
// 2nd command is 'npm i express' ( i and install they both are same thing)
// 3rd start your server in node env -> 'node server.js'

// shipping means making your repo publically available

// you don't need to send node_modules

// you can use nodemon to start server again and again
// without doing manually
// https://www.npmjs.com/package/nodemon